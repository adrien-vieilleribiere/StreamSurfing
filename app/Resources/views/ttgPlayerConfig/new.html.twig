{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>{{ "New Player Configuration"|trans }}</h1>

                {{ form_start(TtgPlayerConfigForm) }}
                {{ form_widget(TtgPlayerConfigForm) }}

                <button type="submit" class="btn btn-primary">{{ "Ok"|trans }}</button>
                {{ form_end(TtgPlayerConfigForm) }}
            </div>
        </div>
    </div>
{% endblock %}