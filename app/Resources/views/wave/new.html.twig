{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>{{ "New Wave"|trans }}</h1>

                {{ form_start(WaveForm) }}
                {{ form_widget(WaveForm) }}

                <button type="submit" class="btn btn-primary">{{ "Ok"|trans }}</button>
                {{ form_end(WaveForm) }}
            </div>
        </div>
    </div>
{% endblock %}