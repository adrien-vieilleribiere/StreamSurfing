{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>{{ "New StreamSurfing"|trans }}</h1>

                {{ form_start(StreamSurfingForm) }}
                {{ form_widget(StreamSurfingForm) }}

                <button type="submit" class="btn btn-primary">{{ "Ok"|trans }}</button>
                {{ form_end(StreamSurfingForm) }}
            </div>
        </div>
    </div>
{% endblock %}