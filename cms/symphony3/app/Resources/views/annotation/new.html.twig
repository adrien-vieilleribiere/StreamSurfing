{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>{{ "New Annotation"|trans }}</h1>

                {{ form_start(AnnotationForm) }}
                {{ form_widget(AnnotationForm) }}

                <button type="submit" class="btn btn-primary">{{ "Ok"|trans }}</button>
                {{ form_end(AnnotationForm) }}
            </div>
        </div>
    </div>
{% endblock %}